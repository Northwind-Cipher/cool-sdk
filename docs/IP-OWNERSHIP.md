# IP ownership inventory

Prepared 2026-09-24 from the repository contents and Git history. This is an inventory of what the repository shows, not legal advice and not a
warranty of title. Where the repository cannot prove ownership, this document says so.

## A. Northwind Cipher original code

Code written for this project and licensed by Northwind Cipher Pvt. Ltd. under the Business Source License 1.1 (see `LICENSE`). These files carry the header
`Copyright (c) 2026 Northwind Cipher Pvt. Ltd.` and `SPDX-License-Identifier: BUSL-1.1`:

- `src/**/*.ts` except the eight vendored files listed in section C (including `src/phala/*`, `src/cli/*`, the witness, consistency and runtime-status modules)
- `tests/**/*.ts`
- `scripts/*`, `examples/**/*.mjs`
- `phala-validation/*.mjs` verification and tooling scripts

Exceptions deliberately left unstamped: `phala-validation/server.mjs` and `phala-validation/witness.mjs` are baked into the container images that produced the
recorded hardware evidence and are kept byte-identical to what ran on the TDX CVMs.

Authorship evidence available in Git: commits by `KenidoesCode` and `Pranauv` (both use the same commit e-mail address), one merge commit by `Sk1zmo` (no content
change), and dependency-bump commits by `dependabot[bot]`. **The repository cannot show whether each contributor's work is owned by Northwind Cipher Pvt. Ltd.**
(employment, assignment or contribution terms are not recorded here). Confirm that before relying on the license change for any contribution.

## B. Third-party dependencies

Installed by npm, not copied into the repository. Inventory and licenses: `docs/THIRD_PARTY_LICENSES.md` (all MIT or Apache-2.0). Their licenses and notices are
unchanged and are not overridden by any Northwind header or license.

## C. Third-party source copied or adapted into the repository

| Files | Stated origin | License | Status |
|---|---|---|---|
| `src/canonical.ts`, `src/hash.ts`, `src/keys.ts`, `src/merkle.ts`, `src/sign.ts` | "Vendored verbatim from `cool-sdk`" (source headers) | Apache-2.0 per the upstream project named in `src/codec.ts` | No Northwind header; original license retained |
| `src/codec.ts` | `cool-sdk` (github.com/KenidoesCode/cool-sdk, Apache-2.0), with one documented modification (browser-safe encoders) | Apache-2.0 | No Northwind header |
| `src/log-memory.ts`, `src/multihash.ts` | "Vendored from `cool-sdk`" | Apache-2.0 per the upstream project | No Northwind header |

The Apache License 2.0 text is preserved in `LICENSES/Apache-2.0.txt` (it was this repository's `LICENSE` before the change). Apache-2.0 obligations that continue to
apply to these files: keep the license and attribution notices, state significant changes, and preserve any upstream `NOTICE` file. No upstream `NOTICE` file is present
in this repository. The `KenidoesCode` account appears to share a commit e-mail address with the project maintainers, but this repository does not establish
that it is controlled by Northwind Cipher Pvt. Ltd.; **confirm the upstream ownership before treating these files as Northwind-owned.** Until then they are treated as
third-party Apache-2.0 code.

Note: these files are Apache-2.0 while the rest of the work is BSL 1.1. The combined distribution is governed file-by-file as marked; recipients receive the BSL work
plus Apache-2.0 components.

## D. Generated artifacts

| Item | Nature |
|---|---|
| `artifacts/**` | Validation evidence generated by running CooL and Northwind's scripts on Phala Cloud (receipts, verification outputs, manifests). Historical record; not edited to look newer. Embeds third-party data: Intel PCK/TCB/QE certificates and collateral, Phala Cloud API responses, dstack agent output |
| `package-lock.json` | Generated by npm |
| `dist/` | Build output; git-ignored and not committed |

## E. Test fixtures

| Item | Nature |
|---|---|
| `tests/fixtures/head.ots.base64` | An OpenTimestamps proof produced by submitting a tree-head digest to public calendars; the proof format and calendar data are third-party |

## F. External / vendor code

None beyond section C. Container images published for the evidence run (Docker Hub, account `pranauvshrinaath`) bundle npm dependencies and a Node.js base image, whose licenses
are listed in `docs/THIRD_PARTY_LICENSES.md`.

## G. Documentation and content

| Item | Note |
|---|---|
| `README.md`, `docs/*.md`, `SECURITY.md`, `CONTRIBUTING.md` | Project documentation; Northwind Cipher content |
| `assets/hero.svg` | Added by `KenidoesCode`; authorship of the artwork is not otherwise recorded |
| README badges | Remote images from shields.io |
| Business Source License 1.1 text | (c) 2024 MariaDB plc; "Business Source License" is a trademark of MariaDB plc; used under its own terms |
| `CODE_OF_CONDUCT.md`, `HACKATHON.md` | Pre-existing project files; unchanged by this pass |

## Names and marks

"CooL" and "Northwind Cipher" are used as product and company names. This repository does not record any trademark registration, and the software license grants no
right to use the names or logos except as the license expressly requires.

## Prior releases

Versions published before this change (including `cool-nwc@3.0.0` on npm and earlier commits) were released under the Apache License 2.0. A later license change cannot
withdraw rights already granted for those versions. Publish the BSL-licensed work under a new version number.
